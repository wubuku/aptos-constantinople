// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosconstantinopledemo.aptos.contract.service;


import com.github.wubuku.aptos.bean.AccountResource;
import com.github.wubuku.aptos.utils.*;
import org.test.aptosconstantinopledemo.aptos.contract.AptosAccount;
import org.test.aptosconstantinopledemo.aptos.contract.ContractConstants;
import org.test.aptosconstantinopledemo.aptos.contract.DomainBeanUtils;
import org.test.aptosconstantinopledemo.aptos.contract.repository.AptosAccountRepository;
import org.test.aptosconstantinopledemo.domain.randomseed.*;
import org.test.aptosconstantinopledemo.domain.*;
import org.test.aptosconstantinopledemo.aptos.contract.RandomSeed;

import java.io.IOException;
import java.math.*;
import java.util.*;
import java.util.function.*;


public class AptosRandomSeedStateRetriever {

    private NodeApiClient aptosNodeApiClient;

    private String aptosContractAddress;

    private AptosAccountRepository aptosAccountRepository;

    private Function<String, RandomSeedState.MutableRandomSeedState> randomSeedStateFactory;


    public AptosRandomSeedStateRetriever(NodeApiClient aptosNodeApiClient,
                                    String aptosContractAddress,
                                    AptosAccountRepository aptosAccountRepository,
                                    Function<String, RandomSeedState.MutableRandomSeedState> randomSeedStateFactory
    ) {
        this.aptosNodeApiClient = aptosNodeApiClient;
        this.aptosContractAddress = aptosContractAddress;
        this.aptosAccountRepository = aptosAccountRepository;
        this.randomSeedStateFactory = randomSeedStateFactory;
    }

    public RandomSeedState retrieveRandomSeedState(String accountAddress) {
        String resourceAccountAddress = getResourceAccountAddress();
        AccountResource<RandomSeed> accountResource;
        try {
            accountResource = aptosNodeApiClient.getAccountResource(resourceAccountAddress,
                    this.aptosContractAddress + "::" + ContractConstants.RANDOM_SEED_MODULE_RANDOM_SEED,
                    RandomSeed.class,
                    null);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
        RandomSeed randomSeed = accountResource.getData();
        randomSeed.setAccountAddress(resourceAccountAddress);
        return toRandomSeedState(randomSeed);
    }

    private RandomSeedState toRandomSeedState(RandomSeed randomSeed) {
        RandomSeedState.MutableRandomSeedState randomSeedState = randomSeedStateFactory.apply(randomSeed.getAccountAddress());
        randomSeedState.setVersion(randomSeed.getVersion());
        randomSeedState.setValue(randomSeed.getValue());
        return randomSeedState;
    }

    private String getResourceAccountAddress() {
        return aptosAccountRepository.findById(ContractConstants.RESOURCE_ACCOUNT_ADDRESS)
                .map(AptosAccount::getAddress).orElse(null);
    }

}

