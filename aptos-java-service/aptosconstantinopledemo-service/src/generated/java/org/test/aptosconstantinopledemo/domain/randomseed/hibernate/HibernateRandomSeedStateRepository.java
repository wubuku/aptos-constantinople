// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosconstantinopledemo.domain.randomseed.hibernate;

import java.util.*;
import java.math.BigInteger;
import java.util.Date;
import org.test.aptosconstantinopledemo.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.test.aptosconstantinopledemo.domain.randomseed.*;
import org.test.aptosconstantinopledemo.specialization.*;
import org.test.aptosconstantinopledemo.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateRandomSeedStateRepository implements RandomSeedStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("AccountAddress", "Value", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted", "Version"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public RandomSeedState get(String id, boolean nullAllowed) {
        RandomSeedState.SqlRandomSeedState state = (RandomSeedState.SqlRandomSeedState)getCurrentSession().get(AbstractRandomSeedState.SimpleRandomSeedState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractRandomSeedState.SimpleRandomSeedState();
            state.setAccountAddress(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (RandomSeedState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{RandomSeedState.SqlRandomSeedState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(RandomSeedState state) {
        RandomSeedState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (RandomSeedState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(RandomSeedState detached) {
        RandomSeedState persistent = getCurrentSession().get(AbstractRandomSeedState.SimpleRandomSeedState.class, detached.getAccountAddress());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().merge(detached);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(RandomSeedState persistent, RandomSeedState detached) {
        ((RandomSeedState.MutableRandomSeedState) detached).setOffChainVersion(persistent.getOffChainVersion());
    }

}

