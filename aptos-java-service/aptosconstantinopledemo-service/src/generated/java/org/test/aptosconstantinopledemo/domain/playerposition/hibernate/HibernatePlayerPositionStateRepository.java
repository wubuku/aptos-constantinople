// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosconstantinopledemo.domain.playerposition.hibernate;

import java.util.*;
import org.test.aptosconstantinopledemo.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.test.aptosconstantinopledemo.domain.playerposition.*;
import org.test.aptosconstantinopledemo.specialization.*;
import org.test.aptosconstantinopledemo.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernatePlayerPositionStateRepository implements PlayerPositionStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("PlayerId", "Position", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public PlayerPositionState get(String id, boolean nullAllowed) {
        PlayerPositionState.SqlPlayerPositionState state = (PlayerPositionState.SqlPlayerPositionState)getCurrentSession().get(AbstractPlayerPositionState.SimplePlayerPositionState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractPlayerPositionState.SimplePlayerPositionState();
            state.setPlayerId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (PlayerPositionState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{PlayerPositionState.SqlPlayerPositionState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(PlayerPositionState state) {
        PlayerPositionState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (PlayerPositionState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(PlayerPositionState detached) {
        PlayerPositionState persistent = getCurrentSession().get(AbstractPlayerPositionState.SimplePlayerPositionState.class, detached.getPlayerId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().merge(detached);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(PlayerPositionState persistent, PlayerPositionState detached) {
        ((PlayerPositionState.MutablePlayerPositionState) detached).setOffChainVersion(persistent.getOffChainVersion());
    }

}

