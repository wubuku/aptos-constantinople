// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosconstantinopledemo.domain.encountertrigger.hibernate;

import java.io.Serializable;
import java.util.*;
import org.test.aptosconstantinopledemo.domain.*;
import java.math.BigInteger;
import java.util.Date;
import org.test.aptosconstantinopledemo.specialization.*;
import org.test.aptosconstantinopledemo.specialization.hibernate.AbstractHibernateEventStore;
import org.hibernate.*;
import org.hibernate.criterion.*;
import org.springframework.transaction.annotation.Transactional;
import org.test.aptosconstantinopledemo.domain.encountertrigger.*;

public class HibernateEncounterTriggerEventStore extends AbstractHibernateEventStore {
    @Override
    protected Serializable getEventId(EventStoreAggregateId eventStoreAggregateId, long version)
    {
        return new EncounterTriggerEventId((Position) eventStoreAggregateId.getId(), BigInteger.valueOf(version));
    }

    @Override
    protected Class getSupportedEventType()
    {
        return AbstractEncounterTriggerEvent.class;
    }

    @Transactional(readOnly = true)
    @Override
    public EventStream loadEventStream(Class eventType, EventStoreAggregateId eventStoreAggregateId, long version) {
        Class supportedEventType = AbstractEncounterTriggerEvent.class;
        if (!eventType.isAssignableFrom(supportedEventType)) {
            throw new UnsupportedOperationException();
        }
        Position idObj = (Position) eventStoreAggregateId.getId();
        Criteria criteria = getCurrentSession().createCriteria(AbstractEncounterTriggerEvent.class);
        criteria.add(Restrictions.eq("encounterTriggerEventId.positionX", idObj.getX()));
        criteria.add(Restrictions.eq("encounterTriggerEventId.positionY", idObj.getY()));
        criteria.add(Restrictions.le("encounterTriggerEventId.offChainVersion", version));
        criteria.addOrder(Order.asc("encounterTriggerEventId.offChainVersion"));
        List es = criteria.list();
        for (Object e : es) {
            ((AbstractEncounterTriggerEvent) e).setEventReadOnly(true);
        }
        EventStream eventStream = new EventStream();
        if (es.size() > 0) {
            eventStream.setSteamVersion(((AbstractEncounterTriggerEvent) es.get(es.size() - 1)).getEncounterTriggerEventId().getVersion().longValue());
        } else {
            //todo?
        }
        eventStream.setEvents(es);
        return eventStream;
    }

}

